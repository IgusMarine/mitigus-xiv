# Engine Windows do Mitigus XIV
# pydivert 3.1.x empacota o WinDivert.dll + WinDivert64.sys (driver assinado) e
# instala o driver no primeiro open(). Importa limpo no Python 3.14.
pydivert>=3.1.3
pywebview>=5.0.0
pystray>=0.19.5
pillow>=10.0.0
bottle>=0.12.25
